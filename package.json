{
  "name": "xhs-paste-image-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension for pasting and dragging images into the Xiaohongshu web creator publish panel.",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "check": "node --check src/content.js && node --check src/background.js && node --check src/ui.js"
  },
  "license": "MIT"
}
